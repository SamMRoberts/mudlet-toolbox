interface:
  display_name: "Mudlet Geyser UI"
  short_description: "Build complete Geyser UI with clean lifecycle"
  default_prompt: "Use $mudlet-geyser-ui to build or repair a Mudlet Geyser interface, selecting the relevant layout, widget, styling, window, persistence, and lifecycle references."
